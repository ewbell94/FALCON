using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//These properties are the same between the VR and MK scene, and are set by outside functions, thus the parent class is needed
public class MovementController : MonoBehaviour
{
    public bool movementActive = false;
}
